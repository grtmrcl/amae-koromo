import { PlayerMetadata } from "../../data/types";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function EstimatedStableLevel(_props: { metadata: PlayerMetadata }) {
  return null;
}
